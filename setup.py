from setuptools import setup, find_packages

setup(
    name="omero-scriptui",
    version="0.1.0",
    description="User Interfaces for running various OMERO scripts",
    packages=find_packages(),
    keywords=["omero"],
)
